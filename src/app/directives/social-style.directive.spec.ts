import { SocialStyleDirective } from './social-style.directive';

describe('SocialStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SocialStyleDirective();
    expect(directive).toBeTruthy();
  });
});
